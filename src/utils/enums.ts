export enum FriendStatusCode {
  ACCEPTED = 'accepted',
  PENDING = 'pending',
  CANCELED = 'canceled',
  DELETED = 'deleted',
}

export enum GroupStatusCode {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
