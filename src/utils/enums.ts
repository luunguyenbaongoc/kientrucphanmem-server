export enum FriendStatusCode {
  ACCEPTED = 'accepted',
  DELETED = 'deleted',
}

export enum GroupStatusCode {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum FriendRequestStatusCode {
  ACCEPTED = 'accepted',
  PENDING = 'pending',
  DECLINED = 'declined',
}
